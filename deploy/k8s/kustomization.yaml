apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Set or override the target namespace here (or in an overlay).
# namespace: remux

resources:
  - configmap.yaml
  - pvc.yaml
  - deployment.yaml
  - service.yaml

images:
  - name: ghcr.io/lostb1t/remux
    newTag: latest
